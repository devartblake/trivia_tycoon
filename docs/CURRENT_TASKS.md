# Current Tasks

_Generated from the markdown files in `docs/` on 2026-05-10._
_PR/branch cross-check pass: 2026-05-10 against `origin/main`, recent remote branches, and known merged PR metadata. No specific active PR number was supplied, so branch heads were also used as the working source of truth._

> This file reconciles the current remaining work from the docs folder. Older completed or superseded checklist items are omitted where newer status docs mark them done.

## P0 - Alpha / Release Blockers

### Runtime validation on device + live backend
- [ ] Validate app launch, auth/bootstrap, login, signup, logout, token refresh, and session resume on a real device/emulator with a live backend.
- [ ] Validate onboarding restore, first challenge completion, reward reveal, and completion handoff to `/home`.
- [ ] Run kids, teen, and adult mode QA for layout, copy, navigation, and mode-specific rendering.
- [ ] Verify all core Synaptix surfaces are reachable: Hub, Arena, Labs, Pathways, Journey, Circles, Command, Store, Rewards.
- [ ] Confirm no high-visibility stale "Trivia Tycoon" labels remain in runtime UI.

### Auth / local web verification
- [x] Add gated live Dart smoke coverage for local Docker/staging auth, CORS preflight, login, `/users/me`, `/users/me/wallet`, refresh, and Spin & Earn confirmed endpoints.
- [x] Confirm `AuthHttpClient` is registered correctly in the Riverpod/provider graph: `authHttpClientProvider` feeds `httpClientProvider`, and `ServiceManager` builds the same backend auth chain.
- [x] Verify login errors for 401, 403, 422, and network timeout with focused `AuthErrorMessages` coverage.
- [x] Verify signup validation and backend error-code display with focused `AuthErrorMessages` coverage.
- [x] Test 401 auto-refresh end-to-end against stub backend coverage in `api_service_test.dart` and gated live refresh coverage in `live_backend_smoke_test.dart`.
- [x] Verify backend profile hydration path after login/signup through `AuthOperations._hydrateProfileFromBackend`; emulator/data-wipe runtime verification remains covered by the live smoke/manual checklist.
- [x] Resolve local web CORS verification: backend Docker CORS config now includes `http://localhost:63033` and `http://127.0.0.1:63033`; `OPTIONS /auth/login` returned `204` with `Access-Control-Allow-Origin` on 2026-05-10.

### Friends, social, and presence verification
- [ ] Live-verify `GET /users/me/friends`, request lists, sent requests, suggestions, authenticated `/users/search`, and authenticated `DELETE /friends`.
- [ ] Two-account/device QA: send request, accept, decline, cancel, block/unblock, unfriend, nickname/favorite state, and DM recipient picking.
- [ ] Presence QA: initial `presence.bulk`, `presence.update` during quiz/match activity, reconnect behavior, and offline transitions on disconnect.
- [x] Remove/deprecate `FriendDiscoveryService`; no `friend_discovery` source file remains in `lib/`.

### Economy, wallet, store settlement
- [x] Map authoritative backend wallet fields from `GET /users/me/wallet`: `credits -> coins`, `neuralXp -> xp`, `synapseShards -> diamonds`.
- [x] Bridge backend wallet refresh into legacy coin, diamond, and XP providers while keeping Hive/local wallet state as cache/fallback.
- [x] Refresh authoritative wallet state after Spin & Earn claim, reward claim, avatar purchase, and payment-return purchase reconciliation.
- [x] Integrate remaining shared wallet counters with authoritative backend state from `GET /users/me/wallet` by syncing legacy `playerCoinsProvider`, `playerGemsProvider`, and Hive wallet cache.
- [x] Add test coverage for displayed wallet field mapping: credits, neural XP, and Synapse Shards after backend wallet fetch.
- [x] Route coin/diamond store purchases through backend `purchaseWithCoinsOrDiamonds` when a SKU exists, then refresh wallet, catalog, and inventory providers; local fallback remains for offline/dev-only catalog items.
- [x] Validate store service settlement contracts for IAP validation, reward claims, PayPal/Stripe return routing, and payment return reconciliation with focused service/screen tests.
- [ ] Run staging settlement smoke: store purchase settlement, inventory refresh, subscription entitlement refresh, IAP validation, Stripe/PayPal return reconciliation, and reward reconciliation after daily/weekly/spin/mission claims. This requires staging credentials/payment test setup and cannot be completed from local code alone.

### Pending database / DevOps actions
- [ ] Apply pending backend EF migrations to staging/prod or confirm already applied: season reward rules, store stock system, flash sales, reward claim rule, effective max quantity.
- [ ] Verify store stock/admin store endpoints behave correctly after migrations.
- [ ] Deploy hosted app-link verification files: `assetlinks.json` and `apple-app-site-association`.
- [ ] Clean native rebuild/reinstall after adding `app_links` to avoid stale `MissingPluginException`.

## P1 - High-Value Implementation

### Crypto economy surfaces
- [x] Add typed crypto service, models, and providers for wallet link, balance, history, withdraw, stake, unstake, staking status, and prize pool actions.
- [x] Base crypto wallet screen exists for balance/history, wallet linking, withdrawals, staking, unstaking, and prize pool interaction.
- [x] Add service/provider tests for crypto endpoint contracts, mutation invalidation, and backend error envelope mapping.
- [x] Add feature flags for staged crypto rollout and safe disabling: `CRYPTO_SURFACES_ENABLED`, `CRYPTO_WRITES_ENABLED`, and `CRYPTO_ENABLED_NETWORKS`.
- [x] Live-validate crypto contracts against local Docker: disposable signup plus balance, history, staking, prize-pool reads, and secure-channel write guard verified against `http://localhost:5000` on 2026-05-10.
- [ ] Run the same crypto contract smoke against staging once staging base URL and credentials are supplied.
- [x] Add/extend crypto UI smoke tests for linked/unlinked, pending withdrawal, disabled-feature, stake/unstake, and prize pool states. (`test/screens/store/crypto_wallet_screen_test.dart` — 8 widget tests covering disabled feature, unlinked, linked, balance, staked units, staking summary, pending withdrawal, empty history)

### Rewards backend integration
- [x] Wire confirmed Spin & Earn endpoints: `GET /arcade/spin/segments` and `POST /arcade/spin/claim`.
- [x] Keep local segment/stat/history fallback when backend Spin & Earn calls fail.
- [x] Add gated smoke coverage for authenticated segment fetch and authorized spin claim route behavior.
- [x] Replace local-only daily reward config and claim state with server endpoints once backend confirms them.
- [x] Replace weekly login streak Hive state with server streak/schedule/claim APIs once backend confirms them.
- [x] Persist spin stats/history server-side when the server stats/history endpoints are available.
- [x] Replace local spin stats/history with server stats/history where available; keep local cache only as offline fallback.
- [x] Fix `hybrid_mission_state.dart` `currentUserIdProvider` stub by using the real profile/auth user provider.
- [x] Wire confirmed mission list and claim flows to backend endpoints: `GET /missions` and `POST /missions/{missionId}/claim`.
- [x] Wire mission progress event submission to `POST /missions/progress/match-completed` and `POST /missions/progress/round-completed` from real gameplay completion events.
- [x] Keep mission swap/generate/delete local-only until backend confirms replacement endpoints.
- [x] Move hardcoded reward step presets behind a provider once a configurable backend endpoint exists.

### Portable avatar persistence
- [x] Avatar upload service accepts `XFile`, sends filename/content length, performs presigned MinIO PUT, and returns the persisted avatar URL.
- [x] `ProfileAvatarController` is wired with upload/profile-sync services and exposes upload progress, error, retry, and remote URL state.
- [x] Profile UI shows avatar upload progress/error/retry state.
- [x] Upload service and controller tests cover success, failure, retry, and persisted URL state.
- [ ] Runtime-verify emulator wipe/login rehydrates avatar from backend URL instead of a local device path.

### Secure channel rollout
- [x] Add `deleteEncrypted` to `EncryptedApiClient` and route DELETE-sensitive social operations through encrypted semantics: `removeFriend`, `cancelFriendRequest`, and `unblockUser`.
- [x] Encrypt selected private social/economy endpoints through the current rollout: friend request send/accept/decline/cancel/remove, block/unblock, loadout save, and Spin & Earn claim.
- [x] Add secure-channel tests for wrong nonce, logout/session clear, and 1 KB/10 KB/100 KB payload coverage.
- [x] Add remaining secure-channel tests for wrong sequence/replay, expiry renewal, reinstall invalidation, and web fallback. (`test/core/security/secure_payload_codec_test.dart` — 15 new tests in 3 groups: replay/sequence, SecureSession model, SecureSessionStore reinstall/web-fallback)
- [ ] Decide and schedule any later refresh/match/economy/messages/private social write expansion beyond the currently selected endpoints.
- [ ] Validate exact backend response schema, replay protection, and sequence semantics against staging.

### Admin security and operations UI
- [ ] Build dead-letter/security event list and replay UI.
- [ ] Build `/admin/audit/security` timeline page.
- [ ] Decide whether central admin notifications should be server-managed or remain local-only.
- [ ] If server-managed, wire `/admin/notifications*` endpoints.
- [ ] Confirm admin auth choices: MFA, token lifetime, refresh rotation, permission scopes, enum values, bulk question mode, event dedupe key, config locality.

### Skill tree / Pathways cleanup
- [x] Normalize branch navigation so cards, route icons, Auto-Path, and search use `/skill-branch/:branchId`.
- [x] Remove duplicate branch overlay rendering from `SkillBranchDetailScreen`; branch detail now uses `AutoPathOverlayPainter`.
- [x] Clarify/rename branch coordinate provider via `branchWorldCentersProvider`; `branchCentersProvider` remains as a compatibility alias.
- [x] Move path recomputation out of build-time mutation in `SkillBranchDetailScreen`.
- [x] Make `MiniHexBranchPreview.fromGraph` use the provided graph through `graphOverride`.
- [x] Replace free-form radial BFS layout with true axial hex-grid positioning: `master_hub` at `(0,0)`, 12 branch roots at ring-2, tier nodes extending along branch direction vectors. `HexMetrics.axialToPixel` converts to world pixels. (`lib/game/controllers/skill_tree_controller.dart`, branch `claude/fix-hexagon-alignment-CrQVu`)
- [ ] Decide whether debug overlay controls ship, hide behind a flag, or move to debug builds only.

## P2 - Quality, Tests, and Release Hardening

### Test coverage
- [x] Add `RichPresenceService` tests for initialization, update, game activity, joinability, watched streams, and dispose.
- [x] Add auth edge-case tests: social login (`getOAuthUrl`), concurrent 401 refresh, offline login (SocketException), logout token cleanup (401 best-effort), expiry detection, metadata extraction. (`test/core/services/auth_service_test.dart`)
- [x] Expand widget tests for `DailyBonusScreen` and `ArcadeMissionsScreen`: renders correctly, coin/gem values, streak states, wallet counters, mission catalog. (`test/arcade/screens/arcade_screens_widget_test.dart`)
- [x] Expand skill tree widget tests: `showPath=0` disables highlight, step 0 label, out-of-bounds step clamps without crash. (`test/screens/skills_tree/skill_branch_detail_screen_test.dart`)
- [x] Expand widget tests for `ArcadeGameShell` — mounts correct game widget per `ArcadeGameId`. (`test/arcade/screens/arcade_game_shell_test.dart` — 5 tests: patternSprint, memoryFlip, quickMathRush mount, ArcadeRunApi accessible via `.of()`, difficulty passed to builder)
- [x] Expand leaderboard interaction tests (`AnimatedRankBadge`, `EnhancedScoreDisplay`). (`test/widgets/leaderboard_widgets_test.dart` — added 6 `AnimatedRankBadge` interaction tests: up/down arrow icons, no-arrow states, animation completion; added 14 `EnhancedScoreDisplay` interaction tests: performance messages for all 5 tiers, percentage label, XP section visibility, XP animation final value, category breakdown, power-up section, class level badge)
- [x] `LoginManager` unit tests: role extraction (`role`/`roles`/`tier` fields), premium status (`isPremium`/`subscriptionStatus`/`tier`), `getNextRoute()` routing, `isLoggedIn()` state, userId persistence. (`test/core/manager/login_manager_test.dart` — 16 tests across 5 groups)
- [x] `AuthTokenStore` + `AuthSession` unit tests: hasTokens, isExpired, role/roles/isPremium/tier getters, copyWith, toJson/fromJson, load/save/clear/updateAccessToken/getRole/isPremium on store — 30+ tests. (`test/core/services/auth_token_store_test.dart`) ✅ NEW
- [x] `AuthStateNotifier` + `AuthState` provider tests: initial state, login, signup, logout, setRole, setPremiumStatus, clearError, `isLoggedInSyncProvider`, `profileSelectedProvider` — 20+ tests. (`test/game/providers/auth_providers_test.dart`) ✅ NEW
- [x] `LeaderboardController` tests: initial state, setCategory, applySorting, category filter (daily/weekly/global), sorting by score/rank, promoteUser, banUser, pause/resume, exportLeaderboardData, isFilterActive, getLeaderboardStats — 25+ tests. (`test/game/controllers/leaderboard_controller_test.dart`) ✅ NEW
- [x] `QuestionController` + `QuestionState` tests: initial(), copyWith, computed properties (accuracy, isQuizOver, currentQuestion), initial provider state, isSelected, selectAnswer (normal/guard/post-reset), reset, usePowerUp — 20+ tests. (`test/game/controllers/question_controller_test.dart`) ✅ NEW
- [x] `UserWallet.fromJson` field mapping tests: credits→coins, neuralXp→xp, synapseShards→diamonds, defaults, doubles, `UserWallet.empty` — 15+ tests. (`test/core/models/user_wallet_model_test.dart` expanded) ✅ NEW
- [x] `CoinBalanceNotifier` tests: initial state, add (accumulate/zero), deduct (sufficient/insufficient/exact), canAfford, set, reset, persistence round-trip — 20+ tests. (`test/game/controllers/coin_balance_notifier_test.dart`) ✅ NEW
- [x] `EnergyState.copyWith` + `EnergyNotifier` tests: initial state, canPlay*/useEnergy/addEnergy/syncWithServer methods, constant validation — 25+ tests. (`test/game/controllers/energy_notifier_test.dart`) ✅ NEW
- [x] `WalletService` tests: addCoins/addGems (positive/zero/negative), spendCoins/spendGems (sufficient/insufficient/exact/zero), setBalances (clamps negatives), coins/gems independence — 20+ tests. (`test/game/services/wallet_service_test.dart`) ✅ NEW
- [x] `currentUserIdProvider` fallback chain tests: stored userId → email prefix → player name → guest; `PlayerProfileService` and `LocalAuthService` integration — 12+ tests. (`test/game/providers/profile_providers_test.dart`) ✅ NEW
- [x] `StatePersistenceService` Hive-backed tests: initialize (fresh box, idempotent), saveAll (each key independently and combined), getGameState/getUserSession/getWebSocketState/getPendingActions (present and absent), getLastSaveTime (null pre-save, valid post-save, updates each call), clearPendingActions/clearTemporaryData/clearAll, hasRecoverableData (crash flag + data combinations), getRecoverySummary, markRecoveryHandled, uninitialized guard — 25+ tests. (`test/core/services/state_persistence_service_test.dart`) ✅ NEW
- [x] `SkillTreeGraph` + `SkillNode` + `SkillEdge` + `SkillNodeCooldown` tests: SkillCategory groupId/brandId for all values, SkillNode.fromJson (all fields, defaults, category/effectType/cooldown/effects parsing, bool-to-num conversion), SkillNode.copyWith, SkillNodeCooldown isOnCooldown/remainingCooldown/canUse, SkillEdge.fromJson, SkillTreeGraph byId/tier/maxTier/getNodeById/nodesInGroup/nodesInBranch/canUnlock/getPrerequisites/getDependents/availableNodes/unlockedNodes/withUnlockedIds/fromJson/copy, subgraphForBranch/indegree/neighbors extension — 60+ tests. (`test/game/models/skill_tree_graph_test.dart`) ✅ NEW
- [x] `LeaderboardEntry.fromJson/toJson/copyWith` tests: all scalar fields (userId int and string, playerName, score, rank, tier, tierRank, booleans, wins, level, country/state/countryCode, xpProgress/accuracy/sessionLength, emailVerified, isBot, subscriptionStatus, averageAnswerTime, engagementScore), DateTime fields (lastActive/timestamp/joinedDate), list fields (interests/powerUps, empty defaults), default fallbacks (Unknown/0/1/active/UTC/free), toJson serialization, copyWith field coverage, fromJson round-trip — 35+ tests. (`test/game/models/leaderboard_entry_test.dart`) ✅ NEW
- [x] `AdaptedQuizState` tests: default constructor values, copyWith for all numeric/bool/string/collection/DateTime/Duration fields, preserves unchanged fields, currentQuestion (null/first/nth/out-of-bounds), totalQuestions, isLastQuestion, scorePercentage (zero/full/partial/empty), quizDuration (null start/start+end/start only), categoryDisplayName — 40+ tests. (`test/game/state/adapted_quiz_state_test.dart`) ✅ NEW
- [x] `AvatarEntry/AvatarPackage/AvatarPackageRenderHints/AvatarPackageMetadata/AvatarPackageInstall/FolderIndex/AvatarPackageManifest` tests: fromJson/toJson/copyWith for all classes, enum parsing (AvatarSource/AvatarKind/AvatarPackageType), override parameters (sourceOverride/kindOverride/packageIdOverride), defaults, installFolderName computed property, FolderIndex.decode from JSON string — 55+ tests. (`test/game/models/avatar_package_models_test.dart`) ✅ NEW
- [x] `QuestionModel` + `Answer` tests: Answer fromJson/toJson, QuestionModel.fromJson (answers-list path: correctIndex from isCorrect/optionId/correctOptionId/expectedAnswer, optionIdByText from answers/id fallback; options-list path: strings and maps; optional fields: imageUrl/mediaKey/audioUrl/tags/reducedOptions/isBoostedTime/isShielded; defaults), _parseDifficulty (numeric/easy/medium/hard/expert/string-number/null/case-insensitive), hasAudio/hasVideo/hasImage/mediaType/isMultimedia, checkAnswer, isCorrectAnswer, optionIdForAnswer, answerTextForOptionId, toJson, copyWith, fromGameplayDto — 55+ tests. (`test/game/models/question_model_test.dart`) ✅ NEW
- [x] `Mission` + `UserMission` tests: _parseMissionType (daily/weekly/seasonal/one_time variants), _parseMissionStatus (completed/complete/expired/swapped/unknown→active), Mission.fromJson (id/missionId fallback, title underscore replacement/key fallback/'Mission' default, progress/total/goal/rewardXp fallbacks, icon mapping for all 8 icons + default, DateTime fields, description/expiresAt optionals), isCompleted/isExpired/progressPercentage, toJson (type.name/status.name), copyWith; UserMission.fromJson (nested mission, claimed/completed flags→completed status, swap_count, assignedAt), isCompleted/canSwap/progressPercentage, copyWith, toJson — 55+ tests. (`test/game/models/mission_model_test.dart`) ✅ NEW
- [x] `PVPChallenge` + `PVPChallengeStatus` + `PVPChallengeResult` tests: PVPChallengeStatus displayName for all 6 values, isActive/isPending/isFinished, PVPChallenge.fromJson (all scalar/DateTime/optional fields, status via byName, wager default), hasWager, isExpired (past/future/finished-status), timeRemaining, getWinnerName (null/challengerName/opponentName), toJson (status.name, ISO dates, conditional optional fields), copyWith; PVPChallengeResult isDraw/scoreDifference (positive difference, negative difference normalized, zero) — 50+ tests. (`test/game/models/pvp_challenge_models_test.dart`) ✅ NEW
- [x] `ReferralCode` + `ReferralInvite` + `ReferralScanEvent` tests: ReferralCode.fromJson (all fields, all 3 status values + unknown fallback, UTC DateTime, optional expiresAt/serverId), toJson/copyWith, Equatable equality; ReferralInvite.fromJson, isExpired (status flag + DateTime.now check), isRedeemed, isPending, daysUntilExpiration/hoursUntilExpiration (0 when expired/redeemed, positive when future), toJson/copyWith; ReferralScanEvent.fromJson (source default "qr"), toJson, Equatable equality — 60+ tests. (`test/game/models/referral_models_test.dart`) ✅ NEW
- [x] `UserPresence` + `PresenceStatus` + `GameState` + `GameActivity` tests: PresenceStatus displayName for all 5 values + unique iconCode verification; GameState displayName/allowsJoining/allowsSpectating for all 5 values; UserPresence.createDefault, fromJson (status via toString for all 5 values + unknown→offline, nested gameActivity), isActive/isAvailable/displayText/statusColor, _formatLastSeen via displayText (just now/Xm/Xh/Xd/over a week), toJson/copyWith; GameActivity.fromJson/elapsedTime/allowsSpectators/canJoin/formattedDuration/toJson/copyWith — 60+ tests. (`test/game/models/user_presence_models_test.dart`) ✅ NEW
- [x] `AdminUserModel` tests: fromJson (all scalar fields, all 4 UserStatus/UserRole/AgeGroup enum values + unknown fallbacks, DateTime fields, winRate double coercion, all optional fields), statusColor/statusText for all 4 statuses, roleColor/roleText/roleIcon for all 4 roles, ageGroupText/ageGroupColor for all 4 age groups, toJson round-trip, copyWith — 55+ tests. (`test/game/models/admin_user_model_test.dart`) ✅ NEW
- [x] `RewardStep` + `RewardType` tests: RewardType.displayName/defaultIcon/defaultColor for all 11 values; RewardStep.fromJson (pointValue as num/int, quantity/description/imageUrl/isLocked/unlockDate/metadata fields + defaults), type parsing (all 11 values + unknown fallback), isUnlocked (locked/future unlockDate/past unlockDate combinations), displayText (quantity=1 plain/quantity>1 appends x{n}), formattedPoints (<1000 plain/≥1000 with k suffix), toJson/copyWith — 55+ tests. (`test/game/models/reward_step_models_test.dart`) ✅ NEW
- [x] `resolveIcon` + `resolveGradient` + `StoreSectionData` + `FeaturedItemData` + `StoreHubStats` + `StoreHubData` tests: resolveIcon for all 17 named icons + null/unknown fallback + custom fallback; resolveGradient (2-color with/without #, 3+ colors, null/empty/short-list fallback, alignment); StoreSectionData.fromJson (all fields, icon/gradient/route defaults); FeaturedItemData.fromJson (all fields, expiresAt via tryParse, invalid date→null); FeaturedItemData.countdownLabel (null/"N days"/"N hours"/"Ending soon" for past); StoreHubStats.fromJson (string/int coercion, all-absent defaults); StoreHubData.fromJson backend format (featured as List, stats computed, categories key trigger); StoreHubData.fromJson legacy format (sections/featured/stats/flashSale); StoreHubData.fallback (5 sections, non-null featured, stats, flashSaleMessage) — 70+ tests. (`test/core/models/store/store_hub_model_test.dart`) ✅ NEW
- [x] `parseGroupId` + `groupAccent` + `branchIdToCategory` + `SkillBranchVM` + `SkillTreeGroupVM` tests: parseGroupId (combat/combat-focused/combat_focused/case-insensitive + unknown fallback); groupAccent for all 4 group IDs; branchIdToCategory for all 13 mapped branches + case-insensitive + unknown; SkillBranchVM computed (totalNodes/unlockedCount/progress); toGraph (node count/IDs/titles/unlocked/category from branchId, edges from requires, tier DFS: root=0/child=1/grandchild=2/diamond-shape longest-path, non-num effects ignored); SkillTreeGroupVM totalNodes/unlockedNodes/progress aggregation across branches — 55+ tests. (`test/game/models/skill_tree_nav_models_test.dart`) ✅ NEW
- [x] `ReactionType` + `MessageReaction` + `MessageReactionSummary` tests: ReactionType.fromCode (all values + null), fromEmoji, isCustom, isGamingSpecific; MessageReaction.fromJson (all fields, all ReactionType values, unknown→thumbsUp, customEmoji), displayEmoji (non-custom/custom/custom-null), toJson (type as code, conditional customEmoji), copyWith; MessageReactionSummary hasReactions/reactionTypes/getCountForType/getReactionsForType/hasUserReacted/getUserReactionType/getUserReaction/getTopReactions (sorted descending, respects limit), getFormattedSummary, getUsersForReaction — 55+ tests. (`test/game/models/message_reaction_test.dart`) ✅ NEW
- [x] `Conversation` + `ConversationType` tests: fromJson (all scalar/DateTime fields, all 5 type values + friend_request/unknown fallback, participantIds as strings/maps/players-key, metadata merging from displayTitle/lastMessagePreview/preview top-level fields, latestMessageId fallback), isGroupChat/isDirectMessage, displayTitle (name/metadata/default), lastMessagePreview (metadata/default), getOtherParticipantId (direct/non-direct/single-participant), toJson (type as name, ISO dates), round-trip, copyWith — 50+ tests. (`test/game/models/conversation_models_test.dart`) ✅ NEW
- [x] `resolveColor` + `ReceivedGift` + `SendableGift` + `GiftHistoryItem` + `GiftStats` + `GiftsData` tests: resolveColor (all 6 color names, hex with/without #, null/empty/invalid→fallback, custom fallback); all model fromJson (all fields, icon/color resolution, type defaults, absent-field defaults); GiftStats coerces int via toString(); GiftsData.fromJson (parsed lists + all-absent defaults); GiftsData.fallback (3 received, 4 available, 3 history) — 60+ tests. (`test/core/models/store/store_gift_model_test.dart`) ✅ NEW
- [x] `StoreAvailabilityState` + `StoreStockState` + `PlayerStoreItem` tests: StoreAvailabilityState.fromJson (all bool fields + saleEndsAt + defaults), always constant; StoreStockState.fromJson (all fields + defaults), isExpired (null/past/future), hasUrgentStock (soldOut/unlimited/qty1/reset<60m/reset>60m), unlimited constant; PlayerStoreItem.fromJson nested format (sku from id fallback, title from name fallback, type from itemType, priceCoins→currency, nested stock/availability), flat backend format (stockState→policy, already_owned→isPurchasable false, discountPercent→isFlashSale, remainingQty=-1→unlimited); isFree/canPurchase computed — 65+ tests. (`test/core/models/store/store_stock_ui_model_test.dart`) ✅ NEW
- [x] `OfferItem` + `FeaturedOffer` + `StoreOffersData` tests: OfferItem.fromJson legacy (sku>id, name>title, price, originalPrice, discount, all optional fields), backend flash-sale format (salePriceCoins/originalPriceCoins/discountPercent); FeaturedOffer.fromJson (endsAt>expiresAt, all fields, defaults), countdownLabel (null/"Expired"/days/hours/minutes/"Ending soon"); StoreOffersData.fromJson (featured/tabs/offers, default 4 tabs), offersForTab, fallback (non-null featured, 4 tabs, all offer tabs in tabs list) — 55+ tests. (`test/core/models/store/store_offer_model_test.dart`) ✅ NEW
- [x] `dailyItemIcon` + `dailyItemColor` + `DailyStoreItem` + `DailyStoreData` tests: dailyItemIcon/dailyItemColor for all 5 categories + null/unknown; DailyStoreItem.fromJson (sku/id fallback, name/title fallback, priceCoins→coins currency, iconPath, category from itemType, nested stock object, flat backend format: resetInterval→per_user/unlimited, soldOut, remainingQty=-1→unlimited), isFree/isCoins; DailyStoreData.fromJson (resetsAt>nextResetAt/24h default, resetIntervalSeconds/bannerMessage), isExpired/timeUntilReset, fallback (4 items, future nextResetAt, 86400 interval) — 60+ tests. (`test/core/models/store/daily_store_model_test.dart`) ✅ NEW
- [x] `AdRemovePlan` + `AdFreeConfig` + `SaleBenefitItem` + `SaleInfoData` + `RewardCard` + `RewardCenterData` + `PremiumStoreData` tests: AdRemovePlan.fromJson (price>priceLabel, durationLabel>title fallback, badge/accentColor/isBestValue/sku), displayTitle/displaySubtitle/tier (elite/premium/null)/billingPeriod (seasonal/monthly/null); AdFreeConfig.fromJson (plans/benefits/title/subtitle), defaultPurchasePlan (bestValue first/first-fallback/null when empty), fallback (3 plans, 4 benefits); SaleBenefitItem.fromJson (icon/value/label/color); SaleInfoData.fromJson (all fields, defaults); RewardCard.fromJson (id/rewardId fallback, reward/rewardLabel fallback, gradient list vs gradientStart/End, isClaimAvailable fallback); RewardCenterData.fromJson (completedCount inferred/explicit, totalCount inferred/explicit); PremiumStoreData.fromJson (null→fallback branches, saleInfo null when absent); fallback assertions — 80+ tests. (`test/core/models/store/premium_store_model_test.dart`) ✅ NEW
- [x] `Message` + `MessageType` + `MessageStatus` tests: fromJson (all scalar fields, senderId→authorId fallback, senderName→senderDisplayName fallback/"Player" default, senderAvatar→avatarUrl, content→body, imageUrl→image, isRead inferred from status=="read"), all 15 MessageType values incl. systemNotification/"system_notification", all 4 status values + unknown fallback, timestamp/createdAtUtc/createdAt fallbacks, metadata (null/non-map→null), hasImage, toJson (type.name/status.name/ISO timestamp), round-trip, copyWith — 55+ tests. (`test/game/models/message_models_test.dart`) ✅ NEW
- [x] `GroupChatInvitation` + `GroupChatMessage` tests: GroupChatInvitation.fromJson (all scalar/DateTime fields, isAccepted/isDeclined defaults), isPending (!accepted && !declined), isExpired (null/past/future), toJson (conditional expiresAt), copyWith; GroupChatMessage.fromJson (all fields, optional replyToMessageId/metadata), toJson (conditional replyToMessageId/metadata/timestamp as ISO), system() factory (id prefix "sys_", senderId="system", isSystemMessage=true, optional metadata) — 55+ tests. (`test/game/models/group_chat_models_test.dart`) ✅ NEW
- [x] `PowerUp` tests: fromJson (all scalar fields, iconPath→icon fallback/"" default, duration→cooldown_seconds→60, price→cost_coins→cost_diamonds→0, currency="diamonds" when cost_diamonds key present), none() factory (id="none"/isNone=true/price=0/duration=0), isNone (false for regular), isActive (>0/0/null), formattedDuration (45s/90s→"1m 30s"/0s/120s/1800s), toJson, copyWith, equality (==+hashCode based on id/name/type/duration) — 55+ tests. (`test/game/models/power_up_test.dart`) ✅ NEW
- [x] `TierModel` tests: fromJson (all scalar fields, isUnlocked/isCurrent defaults, Color(int) colors, IconData(codePoint) icon, unlockedAt DateTime/null), gradient (LinearGradient primaryColor/secondaryColor, topLeft→bottomRight alignment), toJson (icon.codePoint int, color.value int, unlockedAt ISO/null), round-trip, copyWith — 45+ tests. (`test/game/models/tier_model_test.dart`) ✅ NEW
- [x] `CollectionItem` tests: fromJson (all scalar fields, isUnlocked default, unlockedAt DateTime/null, iconPath nullable), rarityColor for all 5 rarities + unknown/case-insensitive, rarityWeight for all 5 rarities + ordering, unlock() (isUnlocked=true, unlockedAt set/preserved, fields preserved, double-unlock stable), toJson (ISO unlockedAt/null), round-trip, copyWith (all fields, preserves unchanged), equality (==+hashCode) — 55+ tests. (`test/game/models/collection_item_test.dart`) ✅ NEW
- [x] `InboxItem` + `InboxType` + `inboxTypeConfig` tests: fromJson (id→notificationId fallback, title→headline, body→summary/message, timestamp→createdAtUtc/createdAt/sentAtUtc fallbacks, actionRoute→route, avatarUrl→imageUrl, icon→iconKey, unread from isRead, payload map/null/non-map), _parseInboxType all aliases (urgent→alert, social/friend_request/friend-request→friend, reward→achievement, update→system, game→challenge, info/unknown→notification, case-insensitive, type/category/kind fields), inboxTypeConfig for all 6 InboxType values (color/icon/label), copyWith — 55+ tests. (`test/core/models/notifications/player_inbox_item_test.dart`) ✅ NEW
- [x] `StoreItemModel` tests: fromJson (all scalar fields, description/""/iconPath/"" defaults, price as int/num round, requiresExternalCheckout/isLimited/isFeatured/owned/quantity/grantQuantity/maxPerPlayer defaults, sortOrder int/null/non-int), currencyType (coins/diamonds/unknown→coins/DIAMONDS case-insensitive); fromStoreCatalog (priceCoins→coins, priceDiamonds→diamonds, priceCoins priority, both-zero→usd+externalCheckout, displayItem price fallback, requiresExternalCheckout override, id/sku fallback, name/displayItem/default, category from itemType, iconPath default per category+displayItem override, grantQuantity/maxPerPlayer, owned param); toJson (duration conditional, round-trip) — 65+ tests. (`test/game/models/store_item_model_test.dart`) ✅ NEW
- [x] `Achievement` tests: constructor invariant (isUnlocked=false clears unlockedAt even when provided; isUnlocked=true without unlockedAt uses DateTime.now()), fromJson (all fields, isUnlocked default, unlockedAt null/present), unlock() (sets isUnlocked/unlockedAt, preserves existing unlockedAt, double-unlock stable), toJson (ISO/null), round-trip, copyWith, equality (==+hashCode) — 30+ tests. (`test/game/models/achievement_test.dart`) ✅ NEW
- [x] `RewardProgress` tests: currentStepIndex (below first/at first/mid/past all/single-step), nextReward (first/second/skips-claimed/all-claimed/empty), pointsToNextReward (exact/partial/null when no next), overallProgress (empty/zero/0.5/1.0/over-max clamped), availableRewards (below threshold/at threshold/excluding claimed/all unclaimed), canClaimReward (true/false-below/false-already-claimed), copyWith — 30+ tests. (`test/game/models/reward_progress_models_test.dart`) ✅ NEW
- [x] `RankedLeaderboardEntry` + `RankedLeaderboardResponse` tests: all fromJson scalar fields (playerId/seasonRank/tier/tierRank/rankPoints/wins/losses/draws/matchesPlayed); response fromJson (seasonId/page/pageSize/total, nested items list, item fields, empty items) — 20+ tests. (`test/game/models/ranked_leaderboard_models_test.dart`) ✅ NEW
- [x] `LeaderboardFilterSettings` tests: fromJson (all bool defaults/true values, deviceType, notificationPreference), toJson (all fields, round-trip), copyWith (all fields, preserves unchanged) — 20+ tests. (`test/game/models/leaderboard_filter_settings_test.dart`) ✅ NEW
- [x] `Challenge` + `ChallengeType` + `ChallengeBundle` tests: fromJson (id/title/description/rewardSummary/progress/completed, all 3 ChallengeType values, icon default), toJson (type.name, progress, round-trip), copyWith (progress/completed, preserves unchanged); ChallengeBundle (challenges list, copyWith replaces challenges/refreshTime, preserves unchanged) — 25+ tests. (`test/game/models/challenge_models_test.dart`) ✅ NEW
- [x] `SeasonPlayer` + `SeasonEndResult` + `SeasonRewardPreview` + `SeasonalTheme` tests: SeasonPlayer.fromJson (all fields, lastActive DateTime); SeasonEndResult (promoted/demoted, hasError false/true, error() factory, hasTiebreakers false/true); SeasonRewardPreview.fromJson (all fields); SeasonalTheme.fromJson (all scalar/DateTime fields, all 3 ThemeType values + unknown fallback, isActive default, optional description/iconEmoji), isCurrentlyActive() (active+in-range/inactive/past-end/future-start), toJson (themeType.name/ISO dates, round-trip) — 40+ tests. (`test/game/models/seasonal_models_test.dart`) ✅ NEW
- [x] `FriendListItemDto` + `FriendRequestDto` + `FriendSuggestionDto` + `PaginatedSocialResponse` tests: FriendListItemDto.fromJson (all fields, displayName/username cross-fallback, isOnline default, lastSeenUtc/sinceUtc DateTime/null, _parseDateTime), toJson round-trip; FriendRequestDto.fromJson (all fields, status default "Pending", DateTime fields, optional sender fields), isPending (Pending/pending case-insensitive/Accepted/Declined), toJson round-trip; FriendSuggestionDto.fromJson (all fields, displayName/username cross-fallback, mutualFriendCount default, reason default), hasMutualFriends (0/positive), toJson round-trip; PaginatedSocialResponse.fromJson (page/pageSize/total, totalPages explicit/inferred, items list, defaults), hasNext/hasPrevious (all cases), toJson — 60+ tests. (`test/core/models/social/social_dtos_test.dart`) ✅ NEW
- [x] `AppSettings` Hive-backed static method tests: daily spin (limit/count/increment/reset/canSpinToday/remaining), last spin date, weekly spin count, lifetime spins, spin reward points (set/get/add/reset), spin booleans (animation/sound/haptic/autoSpin/notification), last spin reward type/value, bonus spin (isBonusSpinActive: null/past/future), spin history (add to front/cap at 50/clear), updateSpinStatistics (totalSpins/rewardCounts/bestReward), spin wheel theme/unlocked themes, splash type (all 5 SplashType values + unknown fallback), QR settings, audio settings, brightness (dark/light), player data (name/progress/quiz progress), onboarding, achievements, purchased songs (purchaseSong/no-duplicate), theme name, primary color, theme presets (save/getAll/delete), confetti settings, jackpot time, badges (unlock/no-duplicate), purchased items, generic helpers (getInt/setInt/getString/setString/getDateTime/setDateTime/getColor/setColor/remove/setStringList/getStringList), segment fetch time, prize log (set/get/filters), total spins/incrementTotalSpins, last spin notification time, depth card theme — 100+ tests. (`test/core/services/settings/app_settings_test.dart`) ✅ NEW
- [x] `ChallengeLivesState` + `ChallengeLivesNotifier` tests: canRevive (true/false/over-limit), isGameOver (all 4 cases: active+0lives+no-revive/active+0lives+revive/active+lives/inactive), copyWith (all fields), constants (kChallengeLivesPerRun==3/kPremiumRevivesPerRun==1), initial state (lives/isRunActive/revivesUsed), startRun (isRunActive/resets lives/resets revives/restores premiumRevivesAllowed), endRun (isRunActive false/reset lives), loseLife (decrement/true when lives remain/false at 0/clamps/false when inactive), useRevive (true/false cases, restores to kChallengeLivesPerRun, increments revivesUsed, canRevive false after), isGameOver integration, loadRunState persistence — 30+ tests. (`test/game/controllers/challenge_lives_notifier_test.dart`) ✅ NEW
- [x] `UserModel` (user_model.dart) tests: fromJson (id/email/isPremium/createdAt/roles, defaults for absent fields, createdAt fallback to now for null/invalid), toJson (all fields, ISO date, round-trip), copyWith (all fields, preserves unchanged) — 20+ tests. (`test/game/models/user_model_test.dart`) ✅ NEW
- [x] `UserModel` (user_profile_model.dart) tests: fromJson (userId/email/isPremium/roles, defaults), constructor defaults (isPremium=false/roles=['player']), toJson (all fields, round-trip), copyWith (all fields, preserves unchanged) — 20+ tests. (`test/game/models/user_profile_model_test.dart`) ✅ NEW
- [x] `PlayerProgress` + `normalizeGameModeName` + `GameMode` tests: PlayerProgress.fromJson (score/streak/defaults), toJson (round-trip); normalizeGameModeName strips "GameMode." prefix/passthrough plain value/empty/partial prefix; GameMode enum has 6 values — 15+ tests. (`test/game/models/misc_game_models_test.dart`) ✅ NEW
- [x] `QuizProgressService` Hive-backed tests: saveQuizProgress/getQuizProgress (store/retrieve/overwrite/empty), setOnboardingCompleted/getOnboardingStatus, savePlayerName/getPlayerName (default 'Player'), savePlayerProgress/getPlayerProgress, updateQuizStats (questionsAnswered/correctAnswers/currentStreak/bestStreak only-higher/averageTime/last_updated), clearAllProgress, getProgressSummary (has_quiz_progress/total_questions/best_streak), autoSave/restoreFromAutoSave (null before save, restored with timestamp), syncProgress/getLastSyncTime (null before/set after), saveCurrentProgress (last_saved+save_reason), daily quiz (null initial/streak=1 first/idempotent same day/lastCompletedDate today), monthly quiz (false/0.0 initial, completed when full/rate 0.5/not-completed partial/months independent), isFeaturedChallengeUnlocked (≥3/below 3), getQuizStats (keys/averageScore=0), updateQuizCompletion (total_quizzes/total_questions/correct_answers/streak/category_stats), getRecentQuizzes/saveCompletedQuiz (default/adds-front/caps-at-10) — 55+ tests. (`test/core/services/settings/quiz_progress_service_test.dart`) ✅ NEW
- [x] `RewardSettingsService` Hive-backed tests: addPurchasedItem/hasItem/getAllPurchasedItems; inventory CRUD (add/no-duplicate/remove/no-op/isInInventory); useItem (insufficient/sufficient/default-count-1), getItemCount; win streak (default 0, set/increment/reset); jackpot (epoch default, setJackpotTime, isJackpotAvailable >24h/<=24h); exclusive currency (default/set/add/spend-sufficient/insufficient/exact); regular currency (default/set/add/spend-sufficient/insufficient); total score (default/set/addScore); daily reward (available-never-claimed/false-today/true-yesterday, getLastDailyReward null/saved, claimDailyReward zero-already-claimed/base-100/streak-bonus/exclusive-at-5); getLastRewardUpdate (null before, set after write); getRewardStats (keys, reflects values); clearAllRewardData (winStreak/currencies/score/purchased/inventory); validateRewardIntegrity (repairs negative winStreak/exclusiveCurrency/regularCurrency, preserves valid) — 65+ tests. (`test/core/services/settings/reward_settings_service_test.dart`) ✅ NEW
- [x] `PlayerProfileService` Hive-backed tests: savePlayerName/getPlayerName (default 'Player'), saveUsername/getUsername (null/set), saveUserId/getUserId (null/set), saveUserRole/getUserRole, saveUserRoles/getUserRoles, setPremiumStatus/isPremiumUser (false/true/unset), saveCountry/getCountry (null-no-op, set), saveAgeGroup/getAgeGroup, saveAvatar/getAvatar (null-no-op, path), isAdminUser (false/legacy-role/roles-list/non-admin), hasRole (match/no-match), saveSynaptixMode, savePreferredHomeSurface, saveReducedMotion/getReducedMotion (false default), saveTonePreference, savePreferredCategories, clearProfile (all 12 fields cleared), loadCompleteProfile (keys present, reflects values), saveProfileBatch (all fields, ignores missing), updateLastActive/getLastActiveTime (null before, set after), validateProfile (has_name false/true, has_user_id false/true), getProfile sync (defaults without box, saved values with box), _calculateRank via level (7 tiers: 0/5/10/20/30/40/50), addXP (increment/level-up/XP-wraps/maxXP-increases/multiple-level-ups) — 60+ tests. (`test/core/services/settings/player_profile_service_test.dart`) ✅ NEW
- [x] `PurchaseSettingsService` Hive-backed tests: addPurchasedItem/hasItem/getAllPurchasedItems (false before/true after/empty/all-items/idempotent); inventory (empty/add/no-dup/remove/no-op/isInInventory false); getItemCount (0/1); purchaseSong/getPurchasedSongs/savePurchasedSongs; addPendingPurchase/getPendingPurchases/clearPendingPurchases (empty/entry/accumulate/clear/timestamp); setLastSyncTime/getLastSyncTime (null before/saved value); getPurchaseStats (totalPurchases/inventoryItems/cacheValid); clearAllData (purchased/inventory/hasItem); validatePurchaseIntegrity (no-error empty, preserves valid); saveState (completes/sets lastSyncTime) — 40+ tests. (`test/core/services/settings/purchase_settings_service_test.dart`) ✅ NEW
- [x] `ProfileStatsService` in-memory tests: recordMatch (stats created/wins+50pts/loss*0.7/draws unchanged), stats accuracy (winRate/averageScore/level=xp/1000+1/levelProgress=(xp%1000)/10), categoryStats (null before/gamesPlayed+highestScore after), streaks (currentWinStreak/longestWinStreak/reset on loss/isStreakActive within 48h), achievements (13 entries/first_win/perfect_game 100%/brain_box score≥1000/getNewlyUnlockedAchievements within 5min), leaderboard (sorted descending/getUserRank 1-indexed/getCategoryLeaderboard), query helpers (getRecentMatches limit/getMatchesByCategory/getUnlocked/LockedAchievements), streams (watchUserStats emits after recordMatch), recordMultipleMatches, getDetailedAnalytics (overview/streaks/achievements/categories/recentPerformance keys) — 50+ tests. (`test/core/services/social/profile_stats_service_test.dart`) ✅ NEW
- [x] `AppCacheService` Hive-backed tests: put/get (round-trip string/int/null→remove/absent→null), set smart dispatch (string/map JSON/null→remove), setJson/getJsonMap/getJsonList (round-trips/absent→null), expiration (expired after duration/available before/setWithExpiration explicit/past DateTime→null), remove (gone after/no-op non-existent), clear (box empty), setTemporaryData/getTemporaryData (round-trip/absent→null/expired→null), clearTemporaryData (removes temp_ prefixed keys), cacheLeaderboard/getCachedLeaderboard (round-trip/empty list), question cache (loadQuestionCache empty/clearQuestionCache), getCacheStats (totalEntries key/count≥1), getCacheEntryInfo (null absent/created+expires+isExpired after put/isExpired false fresh), expireCacheEntry (immediately null on get), extendCacheEntry (still accessible after), getCacheKeysByPattern/removeCacheEntriesByPattern (match/delete/preserve non-matching), cleanOldEntries (removes expired/preserves live), getLastCleanup (null/datetime after clean) — 45+ tests. (`test/core/services/storage/app_cache_service_test.dart`) ✅ NEW
- [x] `EventQueueService` Hive-backed tests: initialize (empty/isInCooldown false/idempotent), enqueueEvent (1 entry/endpoint+payload+timestamp+retry_count=0/accumulates), queue size limit (55→50/FIFO oldest removed), retryQueuedEvents success (queue empty/failures reset to 0), retryQueuedEvents failure (event stays/retry_count++/last_error set), NonRetryableEventException (event dropped permanently), 5 failure cycles→isInCooldown=true, cooldown blocks enqueue (returns false), forceExitCooldown (exits state/enqueue succeeds after), getQueueStatus (is_in_cooldown/consecutive_failures keys/false+0 initially), clearAll (empties queue/resets failures/isInCooldown=false), exportFailedEventsForUpload (player_id/export_timestamp/failed_events keys), state persistence (events survive new instance + initialize) — 40+ tests. (`test/core/services/event_queue_service_test.dart`) ✅ NEW
- [x] `GiftTransactionService` in-memory tests: getCoinBalance (0 unknown/reflects adds/deducts), addCoins (true/balance updated/≤0 false/history), deductCoins (true sufficient/false insufficient/balance unchanged on failure/exact to 0), sendGift (deducts from sender/giftSent type/false if insufficient/no coins to recipient), purchaseTheme/purchaseStickerPack (deducts/false insufficient), purchaseCoins (adds coins), getTransactionHistory (empty/accumulates/limit/most-recent-first), getTransactionsByType (filters/empty when absent), getTransactionsByDateRange (in-range only), TransactionType (isExpense for 4 types/isIncome inverse/displayName non-empty/9 values), getTransactionAnalytics (currentBalance/totalTransactions/net30Days/typeBreakdown), streams (watchBalance/watchTransactions emit after transaction), _loadInitialBalances (current_user=1250/user_1=800/user_2=1500) — 45+ tests. (`test/core/services/advanced/gift_transaction_service_test.dart`) ✅ NEW
- [x] `ThemeSettingsService` Hive-backed tests: defaults after init (brightness=light/name=Default/primary=0xFF2196F3/secondary=0xFF03DAC6/all cached getters), setBrightness/getBrightness (dark/light round-trips/cached updated/persisted), setThemeName/getThemeName (saves/retrieves/cached/persisted), setPrimaryColor/getPrimaryColor (Color.value int/returned as Color/cached), setSecondaryColor/getSecondaryColor (same), saveThemePreset/getAllThemePresets/deleteThemePreset (saves+retrieves/empty before/delete removes/correct fields), applyThemePreset (applies saved values/unknown→defaults), getCurrentTheme (ThemeSettings instance/correct fields), getLastModified (null before/DateTime after set), exportTheme (all 5 keys/name matches/types), importTheme (sets name/primary/brightness/missing-name→Imported), validateThemeIntegrity (no-error valid/repairs missing primary/brightness/name/invalid brightness), getThemeStats (all 5 keys/currentTheme/totalPresets 0 initially/increments/brightness value), saveCurrentTheme (completes) — 50+ tests. (`test/core/services/settings/theme_settings_service_test.dart`) ✅ NEW
- [x] `MessageReactionService` in-memory tests: addReaction (totalCount=1/disabled=false/custom+disabled=false/max limit=false/same user replaces), removeReaction (false unknown/removes/totalCount--/summary removed at 0), removeUserReaction (removes/false when none), toggleReaction (add on first/remove same type/switch type), getMessageReactions (empty/list after add), updateSettings (blocks add/allows custom/max limit/quickReactions), clearMessageReactions (reactions+summary gone), getPopularReactions (sorted descending/limit), getReactionAnalytics (5 keys), getReactionTooltip (""/1 user/"X reacted"/2 users/4+ "others"), canUseCustomReactions (false disabled/false non-premium/true enabled+premium), getSuggestedReactions ('congratulations'→clap/'funny'→laugh/includes quickReactions/no duplicates) — 45+ tests. (`test/core/services/presence/message_reaction_service_test.dart`) ✅ NEW
- [x] `WordSearchController` Flutter-test tests: formattedTime ('00:00' initially/MM:SS pattern), grid initialization (12×12/uppercase letter/wordPositions count/foundWords empty/words stored), words truncated to 8, getCellHighlight (null unfound/Color found), isCellSelected (false initially/true during drag/false after end), onDragUpdate (updates end cell/no-op when not dragging), drag finds word (foundWords updated/getCellHighlight returns color), invalid drag (L-shape finds nothing), reset (foundWords empty/12×12/secondsElapsed=0), onPuzzleComplete callback fires after all words found (500ms delay), WordPosition struct (originalWord/positions/color), isDragging state (false/true during/false after end) — 40+ tests. (`test/game/controllers/word_search_controller_test.dart`) ✅ NEW
- [x] `QuizCategory` enum + `QuizCategoryExtension` + `QuizCategoryManager` tests: enum has exactly 36 values, all values have non-empty displayName/description/datasetName, all return valid Color/IconData, gradientColors always 2 colors; QuizCategoryManager coreCategories (10, contains science), extendedCategories (non-empty, no overlap with core), specializedCategories/educationalCategories (non-empty, contains kids/general), allCategories (no duplicates, count == enum count); fromString (aliases: math/maths→mathematics, tech/computing→technology, cs→computerScience, mixed→general, case-insensitive, empty/unknown→null); getCategoriesForClass (non-empty for all levels, higher-level returns more); searchCategories (partial match, case-insensitive, empty→all, no-match→empty) — 60+ tests. (`test/game/services/quiz_category_test.dart`) ✅ NEW
- [x] `AdaptedQuizState` (quiz_state.dart) tests: default constructor values (20+ fields: questions empty, currentIndex/score/totalXP=0, isLoading/showFeedback/isPaused/isTimerExpired/hasUsedPowerUp/hasUsedExtraTime/isAudioPlaying false, timeRemaining=30, classLevel='1', coins/diamonds/stars/category/error/selectedAnswer null, audioPosition=zero, answerSubmissions empty), copyWith field update for every field, preserves unchanged fields, copyWith chaining; currentQuestion (null when empty/first at index 0/nth by index/null when index>=length), totalQuestions (0/length), isLastQuestion (true for empty list/true at last/false before last), scorePercentage (0.0 empty/100.0 full/50.0 half/0.0 zero score), quizDuration (zero when start null/calculated with start+end/uses now when end absent), categoryDisplayName ('Mixed' when null/contains 'Science'), categoryDescription/categoryColor/categoryIcon fallbacks, questions list/achievements/audioPosition updates — 70+ tests. (`test/game/state/quiz_state_test.dart`) ✅ NEW
- [x] `GroupRole` + `GroupType` + `GroupMember` + `GroupChat` + `GroupChatService` tests: GroupRole 5 values/canManageMembers (owner/admin=true, mod/member/spectator=false)/canSendMessages (all except spectator)/displayName non-empty; GroupType 4 values/displayName non-empty; GroupMember copyWith/toJson/fromJson round-trip; GroupChat memberCount/onlineMemberCount/onlineMembers/admins/getMember (found/null)/canUserPerformAction by role/toJson/fromJson; createGroup (non-null/in allGroups/owner has owner role)/joinGroup (true new member/false already member/false private without invite)/leaveGroup (true/false non-member/false owner)/deleteGroup (true owner/false non-owner)/updateMemberRole/kickMember (true admin/false kicking owner)/updateMemberPresence/getGroup (null/found)/getUserGroups/getPublicGroups/getActiveGameSessions/searchGroups/updateGroupSettings/inviteToGroup/watchGroup stream/getGroupAnalytics/getServiceAnalytics (required keys) — 80+ tests. (`test/core/services/social/group_chat_service_test.dart`) ✅ NEW
- [x] `PVPChallengeStatus` + `PVPChallenge` + `ChallengeCoordinationService` tests: PVPChallengeStatus 6 values/displayName non-empty/isActive (accepted only)/isPending (pending only)/isFinished (completed/declined/expired/cancelled); PVPChallenge hasWager/isExpired/timeRemaining/getWinnerName (null/challengerName/opponentName)/copyWith; getCoinBalance (0 unknown/1250 current_user/800 user_1/1500 user_2); addCoins (true/false for ≤0/negative); createChallenge (non-null pending/retrievable/in getUserChallenges/null if insufficient balance); acceptChallenge/declineChallenge/cancelChallenge/completeChallenge (winner/draw)/submitScore (auto-completes both submitted); getPendingChallenges/getSentChallenges/getActiveChallenges/getCompletedChallenges; getChallengeHistory (respects limit); getChallengeStats (required keys/coinBalance/wins); watchUserChallenges stream; updateSettings/getSettings (maxActiveChallenges/minWager) — 70+ tests. (`test/core/services/social/challenge_coordination_service_test.dart`) ✅ NEW
- [x] `ReadStatus` + `ReadReceipt` + `MessageReadStatus` + `ReadReceiptService` tests: ReadStatus 4 values/displayName (Sent/Delivered/Read/Failed)/isDelivered (delivered+read=true/sent+failed=false)/isRead (read only); ReadReceipt required timestamp field/copyWith preserves unchanged/toJson/fromJson round-trip (with and without error); MessageReadStatus Map receipts/totalRecipients/readCount/deliveredCount (includes read)/isReadByAll/isDeliveredToAll/hasFailures/getReceiptForUser (found/null)/getReceiptsByStatus/copyWith; ReadReceiptService trackMessage (creates entry/all recipients/initialStatus/appears in allMessageStatuses)/updateMessageStatus (updates/no-downgrade read→delivered/no-op unknown message or user)/markMessageAsRead/markMultipleAsRead/updateMultipleStatuses (Dart records batch)/getStatusSummary (Unknown/Sending/Delivered to all/Read by all/Failed)/shouldShowReadReceipts (false untracked/true tracked when enabled/false when disabled)/shouldShowDeliveryStatus/getUnreadCount (0 empty list/counts non-read/0 all read/0 untracked)/getAnalytics (5 keys/totalMessages increases)/updateSettings (readReceiptsEnabled/deliveryReceiptsEnabled/trackMessage no-op when both disabled)/watchReceipts stream (emits after updateMessageStatus/markMessageAsRead)/watchMessageStatus stream (emits after status change) — 60+ tests. (`test/core/services/presence/read_receipt_service_test.dart`) ✅ NEW
- [x] `CustomizationService` tests: default state after initialize (current_user/user_1/user_2 own default theme and emoji_basics pack/unknown user no themes or packs/getActiveTheme returns "default" fallback); purchaseTheme (true sufficient coins/ownsTheme true after/in getOwnedThemes/false insufficient/false already owned/multiple themes same user); applyTheme (false unowned/true owned/getActiveTheme updates/unchanged on fail/theme switching); purchaseStickerPack (true sufficient/ownsStickerPack after/in getOwnedStickerPacks/false insufficient/false already owned); getAvailableStickers (empty fresh user/non-empty current_user/8 from emoji_basics/grows after pack purchase); preferences setPreference/getPreference (default value/null no default/string+int+bool round-trips/getAllPreferences includes set values/empty for fresh user); chatBubbleStyle default "rounded"/setChatBubbleStyle round-trip/independent per user; fontSize default 14.0/setFontSize round-trip; notificationSound default true/setNotificationSound round-trip; watchActiveTheme stream (emits current immediately/emits new theme after applyTheme); watchOwnedStickers stream (emits immediately/emits after purchaseStickerPack); getCustomizationAnalytics (5 keys/ownedThemes≥1/activeTheme non-empty/availableStickers≥8 for current_user/zero for fresh user); exportCustomizations (3 keys/active matches getActiveTheme); importCustomizations (true valid data/themes owned+active after/packs owned after/preferences retrievable/round-trip preserves data); syncCustomizations completes — 65+ tests. (`test/core/services/advanced/customization_service_test.dart`) ✅ NEW
- [x] `EducationalStats` + `SubjectStats` + `EducationalStatsService` Hive-backed tests: EducationalStats defaults (all zeros/empty maps+lists/null lastQuizDate)/copyWith (all fields/preserves unchanged); SubjectStats requires subject/other defaults (quizzesCompleted 0/averageScore 0.0/masteryLevel 1/null lastQuizDate)/copyWith (all fields/preserves unchanged); empty state (all zeros before records); after 1 record (totalQuizzes=1/correctAnswers=score/averageScore=80.0/currentStreak=1/maxStreak=1/lastQuizDate set/recentQuizzes 1 entry with correct score); multiple records (totalQuizzes=3/correctAnswers sums/averageScore 70.0/subjectStats keys per category/Science quizzesCompleted=2/History=1/recentQuizzes≤10); streak (1 after first/same day stays 1/maxStreak≥currentStreak); subject stats (totalQuestions accumulated/correctAnswers accumulated/averageScore calculated/masteryLevel=1 <5 quizzes/lastQuizDate set/subject matches category); history cap at 100 (105 records→100 totalQuizzes); getWeeklyActivity (7 entries/day+quizzes+score keys/valid day abbreviations/0 quizzes before records/1 quiz after record/score is numeric); initialize idempotent — 55+ tests. (`test/game/services/educational_stats_service_test.dart`) ✅ NEW
- [x] `PlayerState` + `QuestionState` + `GameSpectateState` + `SpectateStreamingService` tests: PlayerState all fields/optional defaults (avatar null/correctAnswers 0/streak 0/isAnswering false)/copyWith (score/correctAnswers/streak/isAnswering/id+name/preserves unchanged); QuestionState timeRemaining positive for future start/zero for past start (clamped not negative)/isTimeUp/all fields/playerAnswers empty; GameSpectateState required fields/defaults (hasStarted false/hasEnded false/spectatorCount 0/currentQuestionIndex 0)/progress (0.0 when totalQuestions 0/0.5 at halfway/1.0 complete)/winner; watchGame (returns Stream/creates mock game if missing/Science Showdown/2 players/hasStarted true not ended/10 total questions/has currentQuestion/same stream returned for same gameId); joinSpectate (increments spectatorCount/multiple accumulates/no-op unknown game); leaveSpectate (decrements after leave/no-op unknown); getAvailableGames (returns list/mock games included/only started+not-ended/sorted by spectatorCount desc); sendReaction no-op; mock question (non-empty text/4 options/players named Alex+Jordan/positive scores) — 55+ tests. (`test/core/services/advanced/spectate_streaming_service_test.dart`) ✅ NEW
- [x] `ProfileData` + `MultiProfileService` Hive-backed tests: ProfileData rank (Novice/Enthusiast/Legend by level)/defaults (isPremium false/level 1/currentXP 0/maxXP 500/empty maps/lists)/copyWith (name updated/id preserved/level+XP+maxXP)/toJson+fromJson round-trip; empty state (getAllProfiles empty/getActiveProfile null); createProfile (correct name/non-empty UUID id/createdAt+lastActive set/duplicate name null/max 5 profiles — 6th null); setActiveProfile (true valid id/false unknown/getActiveProfile returns correct after set/switches between profiles); getAllProfiles (returns all created/empty after clearAllProfiles); updateProfile (true persists new name/false unknown id/level+XP updated); deleteProfile (removes from list/returns false for last remaining/active switches to remaining when active deleted); addXPToProfile (increments without level-up/triggers level-up XP>=maxXP/result keys: leveledUp+newLevel+newXP+newMaxXP+xpGained/error map for unknown id); updateActiveProfileGameStats (merges stats into active/false when no active profile); getAccountData+saveAccountData (empty before save/round-trip); getProfileStats (total_profiles+max_profiles+active_profile_id+has_account_data keys/total_profiles reflects count/max_profiles=5) — 50+ tests. (`test/core/services/settings/multi_profile_service_test.dart`) ✅ NEW
- [x] `Achievement` + `AchievementService` Hive-backed tests: Achievement fields/isUnlocked default false/unlockedAt null when not unlocked/unlock() returns unlocked copy preserving id+title/copyWith updates fields/toJson+fromJson round-trip; initialize (opens box/idempotent twice); getUnlockedAchievements (empty before data/returns seeded achievements from Hive/correct count multiple); isAchievementUnlocked (false unknown id/true after seeding/false different id); clearAllAchievementData (getUnlockedAchievements empty after clear/getAchievementStats empty after clear); getAchievementStats (empty map initially/returns seeded stats from Hive); fetchAchievements stub throws (propagates UnimplementedError); saveAchievements throws (documents CastList incompatibility bug in AppSettings fallback) — 25+ tests. (`test/game/services/achievement_service_test.dart`) ✅ NEW
- [x] `ProfileManagerState` + `ProfileAwareXPNotifier` + `ProfileManagerNotifier` + convenience providers tests: ProfileManagerState defaults (profiles empty/isLoading false/error null/isInitialized false/activeProfile null)/copyWith (isLoading/isInitialized/error/profiles/activeProfile/clearActiveProfile/preserves unchanged); ProfileAwareXPNotifier (initial state 0/setXP sets value/setXP(0) sets 0/multiple setXP calls); ProfileManagerNotifier after init (isLoading false/isInitialized true/profiles is List); createProfile (adds to state/isNotEmpty after/isLoading false after); clearActiveProfile (activeProfile null/XP notifier reset to 0); refreshProfiles (completes/isLoading false after); deleteProfile (removes from state/returns false for last profile); switchProfile (activeProfile matches target/isLoading false); convenience providers (isProfileLoadingProvider false/profileErrorProvider null/availableProfilesProvider is List/currentProfileProvider matches manager activeProfile) — 30+ tests. (`test/game/providers/multi_profile_providers_test.dart`) ✅ NEW
- [x] `LogLevel` enum + `LogColors` constants + `LogEntry` + `LogManager` static API tests: LogLevel (5 values/distinct); LogColors constants (all contain ANSI `\x1B[`/non-empty); LogEntry (level/message/source/timestamp fields/toString non-empty); LogManager.log/debug/info/warning/error/performance (no-throw/each adds entry with correct level); getLogs (returns List<LogEntry>/level filter/source filter/both filters); clearLogs (leaves exactly 1 "All logs cleared" info entry/source "LogManager"); exportLogs (non-empty/level filter); exportLogsAsJson (non-empty/contains message text); getLogStatistics (4 required keys: total_logs/by_level/sources/colors_enabled/total matches getLogs/all LogLevel names in by_level/sources includes logged sources/colors_enabled reflects setColorEnabled); timeOperation/timeAsyncOperation (complete without error/add performance entries); setColorEnabled/colorsEnabled round-trip; logStream is broadcast/emits after log(); profile helpers (logProfileCreated/Switched/Deleted/Error/Validation no-throw) — 55+ tests. (`test/core/manager/log_manager_test.dart`) ✅ NEW
- [x] All 17 crypto model files tests: CryptoTransactionKind (7 values/fromApiValue all aliases/unknown for null+empty+garbage/case-insensitive/all have non-empty displayLabel/directions); CryptoTransactionDirection (3 values); CryptoNetwork (4 values/fromKey returns solana default/tryParse null for null+empty+unknown/case-insensitive/isPhaseOne true solana+xrp/false snx+shib/phaseOneNetworks 2 items/all non-empty symbol+displayName); CryptoTransactionStatus (5 values/fromApiValue all/unknown fallback/case-insensitive/isPending true for pending only); CryptoHistoryItem (fromJson required fields/defaults for missing optional/dates parsed/toJson round-trip/isPending delegates); CryptoHistoryResponse (fromJson page+pageSize+total/defaults/totalPages ceil+pageSize-0 returns 1/hasPendingItems false+true/toJson round-trip); CryptoBalanceModel (fromJson all fields/defaults/toJson round-trip); CryptoApiException (code+message/statusCode null default/details empty default/all 6 bool getters true+false/toString includes code+message+statusCode/fromApiRequestException maps fields+default code); CryptoLinkWalletRequest (toJson includes all fields/default network solana); CryptoLinkWalletResult (fromJson all fields/unknown-network defaults to solana); CryptoWithdrawRequest (toJson); CryptoWithdrawResult (fromJson); CryptoStakeRequest (toJson excludes null stakeId/includes non-empty stakeId); CryptoStakeResult (fromJson all fields/numeric defaults); CryptoStakingModel (fromJson all fields/unitType default/toJson round-trip); CryptoPrizePoolModel (fromJson/poolId default "global"/unitType default); CryptoFundPrizePoolRequest (toJson excludes null poolId/includes non-empty); CryptoFundPrizePoolResult (fromJson all fields/poolId default) — 100+ tests. (`test/core/models/crypto/crypto_models_test.dart`) ✅ NEW
- [x] Remaining game model tests: AgeGroup (4 values/value getter/fromString case-insensitive/default general/all non-empty); MatchStatus (5 values/displayText non-empty/value getter/fromString/default waiting); MatchTab (2 values/displayText); MatchFilter (3 values/displayText); LayoutMode (3 values/isMobile+isTablet+isDesktop correct); GameMode (6 values/all expected modes); normalizeGameModeName (strips "GameMode." prefix/passthrough plain/empty/partial); VersusMode (2 values); Member (id/name/avatarUrl null default/stores optional); Participant.isTeam (false no members/false single/true 2+/isHost default false); MatchData fromJson+toJson (required fields/nullable score+avatarUrl/optional score stored/round-trip); JourneyData progress+percentage (0.0/1.0/clamp/0.5/0/100/50/level/nextReward null); ProfileStats.fromMap (level+currentXP+isPremium/defaults for missing); SearchCategory (10 values/all present); SearchResultType (8 values/all present); SearchFilter (defaults empty/copyWith categories+types+includeUnlocked/preserves unchanged fields) — 80+ tests. (`test/game/models/remaining_game_models_test.dart`) ✅ NEW
- [x] `WsState` + `WsEnvelope` + `WsClient` + `WsReliability` tests: WsState (4 values/all distinct/contains disconnected+connecting+connected+reconnecting); WsEnvelope (op+ts+seq null+data null defaults/toJson includes op+ts/excludes null seq+data/includes seq+data when non-null/fromJson round-trip all fields/null seq+data); WsClient construction (initial state disconnected/isConnected false/callbacks no-throw/messageStream is Stream/stateStream is Stream); send while disconnected (calls onError with "Not connected"/no-throw without onError callback); disconnect on disconnected client no-throw; WsReliability (pendingCount 0 initially/handleMessage true for no-seq/true for first seq/false for duplicate seq/distinct seqs both true/sendWithAck increments pendingCount/handleAck removes pending/handleAck unknown id no-throw/reset clears pending/reset clears received seqs/dispose no-throw/sendWithAck adds msgId to data/preserves original data) — 45+ tests. (`test/core/networking/ws_client_test.dart`) ✅ NEW
- [x] `DailyRewardConfigModel` + `DailyRewardStatusModel` + `DailyRewardClaimModel` + `WeeklyRewardDayModel` + `WeeklyStreakDataModel` + `WeeklyClaimResultModel` + `RewardsApiService` tests: DailyRewardConfigModel.fromJson (all fields/default rewardType "coins"/default coinsAmount 100/default displayName "Daily Mystery Box"/default iconName "daily_box"/all defaults applied); DailyRewardStatusModel (holds isClaimAvailable+nextAvailableAtUtc/stores provided DateTime); DailyRewardClaimModel.fromJson (all fields/success false for non-true/numeric defaults/nextClaimAt null when missing/parsed from ISO string); WeeklyRewardDayModel.fromJson (all fields/defaults/amountLabel coins-only/gems-only/combined/both-zero-returns-1); WeeklyStreakDataModel.fromJson (basic fields/defaults/schedule list parsed); WeeklyClaimResultModel.fromJson (all fields/numeric defaults); RewardsApiService (stub throws UnimplementedError for getDailyConfig/getDailyStatus/claimDailyReward/getWeeklySchedule/getWeeklyStreak/claimWeeklyReward) — 45+ tests. (`test/game/services/rewards_api_service_test.dart`) ✅ NEW
- [x] `TierAssigner` + `ReferralCodeGen` + `QrPayload` + `DateFormatter` + `GreetingUtils` tests: TierAssigner (empty list/single entry rank+tier+tierRank/two entries sorted descending/rank-101 tier-9/rank-1001 tier-0/isPromotionEligible true≤25+false>25/isRewardEligible true≤20+false>20); ReferralCodeGen (starts "RC"/10 chars default/Crockford alphabet chars/custom length/two calls differ); QrPayload.buildUri (prefix tt://invite/v=1/rc=code/uid=user/ts=timestamp/no-sig without signature/sig= with signature); DateFormatter (formatDateTime today+yesterday/formatDuration 1:30:05+0:00:00/formatRelative just-now+X-minutes-singular+X-hours+X-days+in-future); GreetingUtils (getGreeting 0/6/11/12/16/17/23 correct strings/getPersonalizedGreeting contains name+"Good"/getMotivationalMessage non-empty/getTimePeriodEmoji non-empty/getTimePeriod one of 4 values/exactly one of isMorning+isAfternoon+isEvening+isNight true) — 40+ tests. (`test/game/utils/game_utils_test.dart`) ✅ NEW
- [x] `QuestionSchemaValidator` + `UnicodeUtils` + `InputValidator` + `CryptoAddressValidator` (extended) tests: QuestionSchemaValidator (valid/empty/all 8 missing required fields/answers non-List/empty List/invalid type/invalid quizFormat/image+video+audio types valid/true_false+fill_in_the_blanks+match_pair formats valid); UnicodeUtils.sanitizeString (empty/ASCII unchanged/unicode preserved); UnicodeUtils.replaceProblematicChars (empty/ASCII unchanged/U+FFFE removed/U+FFFF removed/null byte removed/SOH removed); InputValidator (isValidUnicode normal+empty/cleanInput unchanged+no-throw/safeString unchanged+no-throw); CryptoAddressValidator extended (Solana/XRP/SHIB/SNX empty invalid/valid/invalid cases/validationMessage null for valid+non-null message for invalid Solana+null for valid XRP+null for valid SHIB) — 40+ tests. (`test/core/utils/core_utils_test.dart`) ✅ NEW
- [x] `FlowConnectGridCell` + `Direction` + `FlowConnectPathPoint` + `FlowConnectPathValidator` tests: FlowConnectGridCell (stores all fields/isVisited+isWall+number defaults/copyWith updates+preserves); Direction (5 values/all present); FlowConnectPathPoint (stores row+col+order/fromDirection+toDirection default none/stores custom directions/copyWith updates+preserves); FlowConnectPathValidator.isValidPath (0 points true/1 point true/duplicate coords false/2 adjacent true/diagonal false/out-of-order numbers false/in-order numbers true); FlowConnectPathValidator.checkWinCondition (path shorter false/correct sequence all cells true/wrong number order false/missing last number false) — 35+ tests. (`test/game/logic/flow_connect_logic_test.dart`) ✅ NEW
- [x] `PowerUpEffectApplier` + `SkillCooldownHandler` tests: PowerUpEffectApplier (null→unchanged/id="none"→unchanged/hint→showHint true+other fields unchanged/xp→multiplier 2/boost→isBoostedTime true/shield→isShielded true/eliminate→reducedOptions length 2+contains correct/eliminate 1-option→unchanged/uppercase type lowercased/unknown type→unchanged); SkillCooldownHandler (isOnCooldown false initially/remaining null initially/true after setCooldown/remaining positive after set/clearCooldown removes/clearCooldown unknown no-throw/resetAll clears all/multiple skills independent/clear one preserves others) — 30+ tests. (`test/game/logic/power_up_and_cooldown_test.dart`) ✅ NEW
- [x] `ReferralStorageService` Hive-backed tests: saveReferralCode/getReferralCode (null before save/round-trip/ownerUserId+status+isSynced preserved/overwrite replaces); updateSyncStatus (matching code sets isSynced true/non-matching no-op/sets optional serverId); saveScanEvent/getScanHistory (empty initially/single event/code+source preserved/multiple accumulate); clearReferralCode (null after clear/scan history unaffected); ReferralInviteStorage.saveInvite/getInvites (empty initially/single appears/id+referralCode+status preserved/multiple accumulate/same id updates not duplicates); updateInviteStatus (redeemed/no-op unmatched/expired) — 35+ tests. (`test/game/services/referral_storage_service_test.dart`) ✅ NEW
- [x] `EnergyState` + `LivesState` + `PremiumStatus` + `TierUpdateResult` + `QuestionState` + `TierProgressionState` tests: EnergyState/LivesState (current+max stored/lastRefill null default/lastRefill stored); PremiumStatus (isPremium+discountPercent/expiryDate null default/expiryDate stored); TierUpdateResult (oldTierId+newTierId/tierChanged/hasNewUnlocks false for empty/same ids with tierChanged false); QuestionState (default empty+zero/initial()/currentQuestion null+at-index-0+nth+null-out-of-bounds/isQuizOver true≥length+false+true-empty/accuracy 0.0+1.0+0.5/copyWith all fields/preserves unchanged); TierProgressionState (defaults/copyWith isUpdating+error+preserves/equality equal+different isUpdating+different error/toString/hashCode) — 40+ tests. (`test/game/state/simple_state_classes_test.dart`) ✅ NEW
- [x] `ColorShade` + `shades` + `ColorUtils.shiftHsl/parseHex/blend` + `estimateBrightnessForColor` + `darken` + `lighten` tests: ColorShade (10 values/distinct/contains lightest+darkest); shades map (10 entries/all keys/lightest→50/darkest→900/normal→500/all positive); shiftHsl (returns Color/positive lightens/negative darkens/+1.0 clamps to 1.0/-1.0 clamps to 0.0/zero equivalent); parseHex (#FF0000 red/#00FF00 green/#0000FF blue/#FFFFFF white/alpha 255); blend (white+black+0.0→white/1.0→black/0.5→mid-gray/alpha 255); estimateBrightnessForColor (white light/black dark/yellow light/navy dark); darken (lightness decreases/by-0 equivalent/clamps 0.0); lighten (lightness increases/by-0 equivalent/clamps 1.0) — 30+ tests. (`test/core/utils/color_theme_utils_test.dart`) ✅ NEW
- [x] `GradientThemes` + `DrawerMenuConfig` tests: GradientThemes.getRewardGradient (kids/teens/adults/unknown→adults-fallback); getGreetingGradient (kids/teens/adults/default purple/teens reward≠greeting); static getters (primaryAction/secondaryAction/success/warning/error all LinearGradient 2-colors/distinct first colors); getAgeGroupColors (kids/teens/adults/default all 2-element lists with correct first colors); custom (colors preserved/begin topLeft default/end bottomRight default/custom begin+end); DrawerMenuConfig.mainMenuItems (7 items/all GradientMenuItems/Home route "/"/Arena route "/leaderboard"/non-empty titles+routes/all 2-color gradients/Quiz "/quiz"); moreMenuItems (1 SimpleMenuItem/Missions "/missions"); bottomMenuItems (4 SimpleMenuItems/Settings+Report routes/non-empty); gradient constants (headerGradient 3 colors/backgroundGradient 2/logoutGradient 2/logoutGradient red 0xFFEF4444+darker 0xFFDC2626) — 40+ tests. (`test/game/utils/gradient_drawer_test.dart`) ✅ NEW
- [x] `ThemeMapper` + `ThemeSettings.copyWith` + `ThemeUtils.getAccentColor` tests: ThemeMapper.getThemeForAgeGroup (kids name="Kids"+primaryColor=pinkAccent+light+colorScheme non-null/teens name="Teens"+blueAccent+colorScheme/adults name="Adults"+green+colorScheme/unknown "Defaults"+grey+colorScheme/KIDS case-insensitive/Teens case-insensitive/ADULTS case-insensitive/4 distinct theme names); ThemeSettings.copyWith (updates themeName/primaryColor/secondaryColor/brightness/colorScheme/scaffoldBg/textTheme; preserves unchanged fields; no-args preserves all); ThemeUtils.getAccentColor (kids pinkAccent/teens blueAccent/adults green/unknown blueAccent/empty blueAccent/KIDS case-insensitive/TEENS/ADULTS/kids≠teens/kids≠adults) — 30+ tests. (`test/core/utils/theme_mapper_test.dart`) ✅ NEW
- [x] `LoginUserType` enum + `getAutofillHints` + `getKeyboardType` + `getLabelText` + `getPrefixIcon` tests: LoginUserType (8 values/distinct/all 8 present: email/name/phone/firstName/lastName/text/intlPhone/checkbox); getAutofillHints (email→email/name→username/firstName→givenName/lastName→familyName/phone→telephoneNumber/intlPhone→telephoneNumber/checkbox→email default/text→email default/all non-empty); getKeyboardType (email→emailAddress/name→name/firstName→text/lastName→text/text→text/phone→phone/intlPhone→phone/checkbox→emailAddress/all non-null); getLabelText (email→"Email"/name→"Name"/firstName→"First Name"/lastName→"Last Name"/phone→"Phone"/intlPhone→"Phone"/checkbox→"Email"/text→"Email"/all non-empty); getPrefixIcon (all types→Icon/name+firstName+lastName share icon/phone+intlPhone share icon/email≠phone/name≠email icons) — 40+ tests. (`test/core/utils/text_field_utils_test.dart`) ✅ NEW
- [x] `Answer` + `GameBadge` + `PlayerProgress` + `FlowConnectLevelData` + `MenuSection`/`MenuItemType` tests: Answer (text+isCorrect stored/fromJson parses/isCorrect defaults false/toJson/round-trip); GameBadge (5 fields stored/isUnlocked defaults false/fromJson all fields+default false/toJson all keys/round-trip); PlayerProgress (score+streak stored/fromJson parses+defaults 0/toJson/round-trip); FlowConnectLevelData (grid+gridSize+totalNumbers+solutionPath stored); MenuSection (4 values/distinct/main→"Main Menu"/more→"More Options"/bottom→"Settings"/logout→"Logout"/all non-empty+distinct displayNames); MenuItemType (2 values/gradient+simple/distinct) — 30+ tests. (`test/game/models/answer_badge_progress_test.dart`) ✅ NEW
- [x] `FlowConnectGameStatus` + `FlowConnectGameState` tests: FlowConnectGameStatus (4 values/distinct/notStarted+playing+success+failed); FlowConnectGameState (stores all fields/canUndo false fresh/canRedo false fresh/copyWith updates gridSize+totalNumbers+currentNumber+isComplete+status+currentPath+pathHistory/preserves unchanged fields); recordState (canUndo true/canRedo false at end); undo (no-op at start/canRedo true after undo/canUndo false after undo from 2nd/restores prior state); redo (no-op at end/canRedo false after undo+redo/canUndo true after undo+redo); trimHistory (canRedo false after trim at end/canRedo false after trim after undo/canUndo false after trim after undo); == (same-field states equal/differ by currentNumber not equal/differ by gridSize not equal); hashCode (equal states same hash) — 35+ tests. (`test/game/state/flow_connect_game_state_test.dart`) ✅ NEW
- [x] `ModeCostDto` + `EconomyStateDto` + `SessionStartDto` + `DailyTicketClaimDto` + `ReviveQuoteDto` + `PityResponseDto` tests: ModeCostDto (stores all fields/adjustedCost null default/effectiveCost returns adjustedCost or baseCost/hasDiscount true<base/false null/false>=base/fromJson all fields+defaults/toJson omits adjustedCost when null/includes when non-null/round-trips); EconomyStateDto (fromJson all 8 fields/modes deserialized to ModeCostDto/empty modes/toJson all fields+nested maps); SessionStartDto (fromJson discountApplied false/adjustedCosts empty/parses nested ModeCostDto/toJson both fields); DailyTicketClaimDto (fromJson defaults/denyReason null/toJson omits null denyReason/includes non-null); ReviveQuoteDto (stores all fields/costCurrency defaults coins/hasDiscount true+false+false/toJson 4 fields); PityResponseDto (fromJson false+0 defaults/parses values/toJson both fields) — 50+ tests. (`test/core/dto/economy_dto_test.dart`) ✅ NEW
- [x] `WalletDto` + `PlayerDto` tests: WalletDto (stores coins+gems/fromJson parses+defaults 0/toJson both keys/round-trip); PlayerDto.fromJson (id/username/email/handle/avatarUrl/country/ageGroup/role/xp/level/wallet parsed/avatarUrl null when absent/country null when absent/ageGroup defaults general/role defaults user/wallet as nested WalletDto/createdAt from ISO string/invalid createdAt fallback to DateTime); toJson (id/username/wallet as nested map/createdAt as ISO string/avatarUrl+country keys present) — 30+ tests. (`test/core/dto/player_dto_test.dart`) ✅ NEW
- [x] `QuestionAnswerSubmission` + `QuestionAnswerCheckResult` tests: QuestionAnswerSubmission (stores selectedAnswer/stores question reference/question.id accessible/wrong answer preserved); QuestionAnswerCheckResult (stores questionId/selectedAnswer/isCorrect true+false/source defaults backend/metadata defaults empty/correctAnswer defaults null/explicit source stored/explicit correctAnswer stored/explicit metadata stored/multiple optional fields together) — 20+ tests. (`test/core/models/question_validation_models_test.dart`) ✅ NEW
- [x] `GameEventDto`+`GameEventLeaderboardEntryDto`+`SeasonDto`+`PlayerSeasonStateDto`+`GuardianDto`+`MyGuardianStatusDto` tests: GameEventDto (all 8 fields/status defaults upcoming/startsAt as DateTime/entryFee+aliveCount default 0/toJson all keys+ISO date); GameEventLeaderboardEntryDto (all fields/isEliminated false/toJson 5 keys); SeasonDto (id+name/isActive false default/dates as DateTime/toJson ISO+round-trip); PlayerSeasonStateDto (tier defaults 1/others 0/toJson 7 keys); GuardianDto (avatarUrl null/tier 1/lastChallengedAt null+parsed+toIso); MyGuardianStatusDto (isGuardian false/tier null/currentMatchId null/toJson 5 keys) — 40+ tests. (`test/core/dto/game_event_season_guardian_dto_test.dart`) ✅ NEW
- [x] `TileDto`+`TerritoryBoardDto`+`DuelResultDto`+`VoteResultDto`+`PlayerNotificationDto`+`MatchUpdateDto`+`GameEventEliminationDto`+`GameEventClosedDto`+`GuardianChangedDto`+`TerritoryCaptureDto`+`VoteTallyUpdatedDto`+`DirectMessagesUpdatedDto` tests: TileDto (ownerId null/xpMultiplier 1.0/toJson 6 keys); TerritoryBoardDto (tierNumber 1/tiles empty/deserialized as TileDto/toJson nested list); DuelResultDto (matchId+tileId/toJson); VoteResultDto (tally as Map<String,int>/winningChoice null/toJson 4 keys); hub DTOs (all fromJson defaults/DateTime fallbacks/payload null/tally empty); DirectMessagesUpdatedDto (all defaults+DateTime fallback) — 50+ tests. (`test/core/dto/territory_hub_vote_dto_test.dart`) ✅ NEW
- [x] `difficultyLabel`+`difficultyColor`+`ModuleDto`+`LessonOptionDto`+`LessonDto`+`ModuleCompleteResponseDto` tests: difficultyLabel (1→Easy/2→Medium/3→Hard/4→Expert/other→Unknown); difficultyColor (1→green/3→orange/4→red/other→grey); ModuleDto.fromJson (id+title/description empty/difficulty 1/isCompleted false/difficultyText Easy+Expert/difficultyColour green+red); LessonOptionDto (id+text); LessonDto (lessonId+order 0 default/options deserialized/empty options/explanation null); ModuleCompleteResponseDto (all 7 fields/numeric defaults/isFirstCompletion true Completed/false other) — 35+ tests. (`test/core/dto/learning_dto_test.dart`) ✅ NEW
- [x] `StudyOption`+`StudySetListItem`+`StudyQuestion`+`StudySetDetail`+`StudySessionMode`+`FlashcardAction`+`StudySession` tests: StudyOption (id+text/defaults empty); StudySetListItem (id+title/description empty/kind Category/questionCount 0); StudyQuestion (id+text+category/difficulty Easy/options deserialized+empty/correctOptionId+explanation+mediaKey null); StudySetDetail (questions deserialized+empty); StudySessionMode (2 values/apiValue SelfTest+Flashcard); FlashcardAction (4 values/apiValue Again+Hard+Good+Easy); StudySession.fromJson (mode Flashcard→flashcard/other→selfTest/counts 0/isCompleted false/questionIds+answeredQuestionIds lists+empty) — 40+ tests. (`test/core/dto/study_dto_test.dart`) ✅ NEW
- [x] `PlayerMindProfileDto`+`CoachBriefDto`+`PlayerRecommendationDto`+`PlayerHomePersonalizationDto`+`ExperimentAssignmentDto`+`PlayerExperimentsDto`+`SingleExperimentResultDto`+`BehaviourEventDto` tests: PlayerMindProfileDto (all defaults/playerId/confidenceLevel/categoryStrengths Map/non-Map→{}/shouldShowRetentionNudge ≥0.8/shouldDisableHardMode ≥0.75/shouldReducePushBadges ≥0.7); CoachBriefDto (id null/tone encouraging/explicit tone); PlayerRecommendationDto (source sidecar/priority 1/score 0.0/expiresAt null); PlayerHomePersonalizationDto (mode+difficulty defaults/recs deserialized/coachBrief null+parsed/topRecommendations sorted+capped 3); ExperimentAssignmentDto (variantKey control/getBool+getString+getInt correct+fallbacks); PlayerExperimentsDto (empty+deserialized); SingleExperimentResultDto (enrolled false/assignment null+parsed); BehaviourEventDto (auto-occurredAt/toJson omits null+empty/includes non-null/non-empty) — 60+ tests. (`test/core/dto/personalization_dto_test.dart`) ✅ NEW
- [x] 25 `game_bonus_providers` StateProvider tests: initial values (int→0/double→1.0 or 0.0/bool→false/Map?→null); mutation via notifier.state; container isolation (independent copies per ProviderContainer); cross-provider independence (setting one bool does not affect another); categoryBonusProvider null+map+clear-to-null; streakMultiplierProvider+scoreBonusMultiplierProvider+speedBonusMultiplierProvider 1.0 initial — 30+ tests. (`test/game/providers/game_bonus_providers_test.dart`) ✅ NEW
- [x] `GeneralKeyValueStorageService` Hive-backed tests: setString/getString (round-trip/overwrite/null-for-missing); setInt/getInt (round-trip/0-default/overwrite); setBool/getBool (true+false/null-missing/toggle); setColor/getColor (Colors.red+arbitrary ARGB/null-missing/overwrite); setDateTime/getDateTime (ISO8601 round-trip/null-missing/overwrite); setStringList/getStringList (list round-trip/single-element/null-missing/overwrite); setJson/getJson (simple map/null-missing/non-JSON→null/nested map); generic get/set; remove (deletes key/non-existent no-throw); key isolation — 30+ tests. (`test/core/services/settings/general_key_value_storage_service_test.dart`) ✅ NEW
- [x] `ConfigStorageService` Hive-backed tests: saveConfig/getConfig (string+int+bool+null-for-missing+overwrite+map); removeConfig (existing/non-existent no-throw/does-not-affect-others); getAllConfigKeys (empty/all 3 keys/excludes removed); clearAllConfigs (empty after/null after/can-save-after-clear) — 20+ tests. (`test/core/services/storage/config_storage_service_test.dart`) ✅ NEW
- [x] Web account linking (3 methods — QR code + Google Sign-In + one-time link code): added `google_sign_in: ^6.0.0` to pubspec.yaml; `WebLinkDto` models (QrTokenResponse/QrStatusResponse/QrLinkStatus enum/LinkCodeResponse/GoogleWebAuthResponse with fromJson); `WebLinkService` HTTP client (generateQrToken/pollQrStatus/consumeQrToken for QR; authenticateWithGoogleToken for Google; generateLinkCode/consumeLinkCode for codes; WebLinkException); `webLinkServiceProvider` (Riverpod, wired to EnvConfig.apiBaseUrl); `QrLinkWidget` (web: generates QR token, renders QrImageView, polls every 3s, onSuccess callback); `LinkCodeScreen` (mobile: generates 6-char code with countdown timer, copy-to-clipboard, regenerate); `LoginScreen` — web section with Google Sign-In button (_handleGoogleWebSignIn), link code input field (_handleLinkCodeSubmit), QR code toggle (show/hide QrLinkWidget). All 3 methods exchange backend session tokens and update isLoggedInSyncProvider. (`lib/core/dto/web_link_dto.dart`, `lib/core/services/web_link_service.dart`, `lib/game/providers/web_link_providers.dart`, `lib/screens/web_link/qr_link_widget.dart`, `lib/screens/web_link/link_code_screen.dart`, `lib/screens/login_screen.dart`) ✅ NEW
- [x] Mobile device auth (Apple Game Center + Google Play Games Services): added `games_services: ^4.0.0` to pubspec.yaml; `GamePlatformIdentity` model (platform/playerId/displayName/toJson); `GamePlatformAuthService` (signInSilently→GamePlatformIdentity?/isSignedIn/currentPlatform; kIsWeb guard; catches exceptions); `AuthApiClient.loginWithGamePlatform` + `linkGameAccount` (new POST endpoints /auth/mobile-game-login + /auth/link-game-account); `BackendAuthService.loginWithGamePlatform` + `linkGameAccount`; `gamePlatformAuthServiceProvider` (Riverpod Provider); `AuthOperations.trySilentGameLogin` (returns bool, fires on initState) + `loginWithGamePlatform`; `LoginScreen` — `_trySilentGameLogin` called in initState when backend auth enabled on iOS/Android, `_handleGamePlatformLogin` method, `_buildNativeGameLoginButton` shown conditionally (iOS: "Continue with Game Center", Android: "Continue with Play Games"). (`lib/core/services/game_platform_auth_service.dart`, `lib/core/services/auth_api_client.dart`, `lib/core/services/auth_service.dart`, `lib/game/providers/auth_providers.dart`, `lib/screens/login_screen.dart`) ✅ NEW
- [x] Server asset download service: `AssetManifestEntry` model (key/url/sha256/version/sizeBytes/fromJson/toJson); `DownloadedAssetRecord` model (key/version/sha256/downloadedAt/fromJson/toJson); `AssetDownloadService` (syncInBackground/checkForUpdates/downloadAsset with SHA-256 verify/loadAsset/loadAssetOrBundle-falls-back-to-rootBundle/clearDownloadedAssets; Hive box `asset_downloads`; baseDirOverride for testing; key sanitisation); test: 20+ tests covering model round-trips/checkForUpdates returns stale entries/HTTP error returns empty/downloadAsset writes file+verifies hash+throws on mismatch+throws on 404+skips empty hash/loadAsset null for missing+content after download/loadAssetOrBundle returns local when present/clearDownloadedAssets removes files+Hive records/key sanitisation for path separators. (`lib/core/models/asset_manifest_entry.dart`, `lib/core/services/asset_download_service.dart`, `test/core/services/asset_download_service_test.dart`) ✅ NEW
- [ ] Move toward 90%+ coverage of `lib/game/` and `lib/core/` (widget tests, HTTP mocks, Hive services, provider integration tests).

### Dependency and build health
- [ ] Run `flutter pub outdated` and apply safe security/minor updates.
- [ ] Run `flutter pub deps --style=compact` and remove unused dependencies from retired features.
- [ ] Install `nuget.exe` on Windows PATH; verify `nuget help`, `flutter clean`, `flutter pub get`, and `flutter build windows`.
- [ ] Configure CI to enforce analyze/tests/coverage and prevent raw production `debugPrint` regressions.

### Product polish
- [ ] Expand sound cues beyond Hub to Arena, Labs, Pathways, Journey, Circles, and Command.
- [ ] Standardize SFX taxonomy, shared cue helper, volume profiles, settings gating, fallback behavior, and cue tests.
- [ ] Run low-end Android and iOS audio latency/fatigue QA.
- [ ] Complete final empty-state copy sweep and mode-specific accessibility pass.
- [ ] Verify frontend labels, preferences payloads, and analytics payloads against backend dashboards/docs.

### Questions, Play, Learn, Study cleanup
- [x] Align gameplay question parsing to backend `GameplayQuestionDto`: `text`, `options`, `mediaKey`, no embedded correctness, and `Easy/Medium/Hard/Expert` difficulty mapping.
- [x] Route single-player, category, class, and multiplayer question loading through `QuestionHubService` and `GET /questions/set` with the correct `mode` contract.
- [x] Keep class/grade gameplay as frontend category+difficulty mapping; no nonexistent `/questions/classes/*` gameplay endpoint is required.
- [x] Make multiplayer use `mode=ranked`, count-only, and no `playerId` personalization for fair question sets.
- [x] Remove stale direct multiplayer `/api/questions` fetch fallback; repository/hub is primary and local mock fallback remains only after repository failure.
- [x] Use `/questions/check` and `/questions/check-batch` as the correctness source for backend questions, posting `selectedOptionId` and mapping `correctOptionId` back to answer text.
- [x] Extend gated live smoke coverage for `/questions/set`, `/questions/check`, and `/questions/check-batch`.
- [ ] Live-verify gameplay stays on backend question data in local/staging/prod.
- [ ] Run targeted Flutter/Dart tests for question model parsing, service query contracts, category/class launch, and multiplayer routing once SDK tooling is available.
- [ ] Decide whether local question fallback remains enabled in production after backend parity is proven.
- [ ] Add source observability beyond banners/logging: backend success ratio, latency, and coverage drift.
- [ ] Introduce `/play` route aliases, remove or redirect frontend `/quiz/*` routes where safe, and align labels from Quiz to Play.
- [ ] Create one launcher/orchestrator for route params to question session state and remove ambiguous router imports.
- [ ] Add learning progress summary, recommended module logic, continue-learning CTA, reward transparency, and idempotent lesson/module completion retries.
- [ ] Remove deprecated `ApiService` and `SynaptixApiClient` quiz methods after all callers are migrated; active gameplay callers are no longer using `ApiService.fetchQuestions()`.
- [ ] Update route maps and tests.

## P3 - Deferred / Decision-Gated

### Cross-check notes
- [ ] If there is a specific active PR number, re-run this cross-check against that PR number's changed files and patches.
- [ ] Known merged PR metadata was spot-checked, including PR #158 for Phase 3 test coverage pass 2.
- [ ] Recent remote branches indicate several task slices may also be PR-style branch work, especially `copilot/stn-*` skill-tree branches and `claude/fix-hexagon-alignment-CrQVu`.

### Packet E package root rename
- [ ] Wait for product/legal store transition plan before changing package root or bundle IDs.
- [ ] Rename `pubspec.yaml` package from `trivia_tycoon` to `synaptix`.
- [ ] Update all `package:trivia_tycoon/...` imports.
- [ ] Change Android application ID and iOS bundle identifier to `com.theoreticalmindstech.synaptix`.
- [ ] Regenerate Firebase/Google service configs and `build_runner` outputs.
- [ ] Document rollback strategy and run the rename in an isolated branch.

### Backend Packet E
- [ ] Defer backend namespace rename from `Tycoon.Backend.*` to `Synaptix.Backend.*` until after Alpha/stable release.
- [ ] Later rename service/telemetry identifiers, Docker/CI labels, Elasticsearch aliases, and related ops naming.

### Optional ML / personalization UX
- [ ] Keep personalization frontend paths on the current `/personalization/{playerId}/...` contract and clean up older docs that show superseded paths.
- [ ] Decide whether churn-risk and match-quality ML signals need visible UX in this sprint.
- [ ] If yes, add or expand typed providers/UX for ML-driven nudges behind feature flags.
- [ ] Track `source` values such as `deployed-model` vs `heuristic` only as optional telemetry/UX context.

### Backend-dependent future APIs
- [ ] Do not wire `GET /v1/assets/audio/{category}/{filename}` until backend marks it live.
- [ ] Treat premium DB-backed catalog phase 2 and richer gift/cosmetic/avatar purchase contracts as post-launch unless reprioritized.
- [ ] Add stronger store feature flags via `/store/system/status` if backend exposes the endpoint.
